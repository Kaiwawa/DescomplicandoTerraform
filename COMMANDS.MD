# Create a pre-built Terraform environment using Docker.
--> docker run -it -v .:/app -w /app --entrypoint "" hashicorp/terraform:light sh

# Terraform 
- export AWS_ACCESS_KEY_ID=
- export AWS_SECRET_ACCESS_KEY=
